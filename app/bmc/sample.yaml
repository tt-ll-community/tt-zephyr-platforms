sample:
  name: JTAG
tests:
  sample.app:
    sysbuild: true
    extra_configs:
      - CONFIG_TT_FWUPDATE=n
    platform_allow:
      - tt_blackhole/tt_blackhole/bmc
    tags: e2e
    harness: console
    harness_config:
      type: one_line
      regex:
        - "BMFW VERSION (.*)"
