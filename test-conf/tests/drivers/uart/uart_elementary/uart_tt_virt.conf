CONFIG_ZTEST=y

# Required by uart_elementary test suite. We only simulate interrupts currently.
CONFIG_UART_INTERRUPT_DRIVEN=y

# Required by uart_elementary test suite, otherwise test_uart_proper_configuration fails.
CONFIG_UART_USE_RUNTIME_CONFIGURE=y
